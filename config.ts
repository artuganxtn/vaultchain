
export const config = {
  // --- Global Configuration ---
  
  // The base URL for your backend API.
  // In this simulated environment, this is intercepted by our mock API client.
  // In production, this would point to your live server (e.g., https://api.vaultchain.app/v1).
  API_BASE_URL: '/api/v1',
};
